// backend/src/routes/post.js
const express = require('express');
const router = express.Router();
const Post = require('../models/Post');

// Define routes here (GET, POST, PUT, DELETE)

module.exports = router;
